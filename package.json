{
  "name": "todo_app",
  "version": "1.0.0",
  "description": "Basic todo app with modern frameworks (React, node.js, postgres)",
  
  "scripts": {
    "build": "if-env TYPE=server && (cd server && npm install) || (cd client && npm install)",
    "server": "node ./server/index.js",
    "client": "cd client && npm start" },
  "author": "Noel Conlisk",
  "license": "ISC",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
